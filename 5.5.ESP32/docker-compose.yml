services:
  app:
    image: esp32
    build:
      context: ./esp32
      dockerfile: dockerfile
    ports:
      - "3000:3000"    # Vite app
      - "3001:3001"    # API server
    volumes:
      - ./esp32/ssl:/app/ssl
    # ...existing configuration if any...
